<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pgr4.univo.com.service_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash_screen"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".RecuperarContrasenaActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".LlenadoActivity" />
        <activity android:name=".FacturacionActivity" />
        <activity android:name=".MostrarCategoriasActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".NavegadorActivity"
            android:label="@string/title_activity_navegador"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PagoFacturaActivity" />
        <activity
            android:name=".navegadordos"
            android:label="@string/title_activity_navegadordos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".menu_principal"
            android:theme="@style/fondoinicio" />

        <activity
            android:name=".activity_categorias"
            android:theme="@style/fondocategorias" />

        <activity android:name=".contrataciones"
            android:theme="@style/fondocontrataciones"/>

        <activity android:name=".favorito"
            android:theme="@style/fondofavorito"/>

        <activity android:name=".acercade"
            android:theme="@style/fondoacercade"></activity>
    </application>

</manifest>